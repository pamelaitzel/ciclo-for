#include <stdio.h>
#include <stdlib.h>
#include <string.h>
float promedio(float a , float b)
int main() {
    float a=5, b=10, prom;
    prom=promedio(a,b);
    ṕrintf("el promedio es: %"2)
    return 0;
}